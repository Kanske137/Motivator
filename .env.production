# Public build-time vars for the hosted (Cloudflare Pages) build.
# VITE_SHOPIFY_API_KEY = the Wallery app's Shopify Client ID (API key). This is
# a PUBLIC value (it appears in the App Bridge script tag and OAuth URLs) and is
# a single constant for the whole app, not per-merchant. Safe to commit.
VITE_SHOPIFY_API_KEY=7e9f86a69ad4625a163389a4163c8053
